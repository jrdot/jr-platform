# 전역 에이전트 지침

이 저장소는 여러 AI 에이전트가 공통으로 참고할 수 있는 베이스 플랫폼 예제입니다.

## 기본 원칙

- 사용자 목표를 최우선으로 한다.
- 작업 범위를 불필요하게 확대하지 않는다.
- 추측보다 확인을 우선한다.
- 변경 전 현재 상태를 확인한다.
- 파괴적 작업은 명시적 승인 없이 수행하지 않는다.
- 가능한 경우 결과를 검증한 후 완료로 간주한다.
- 실패, 제약, 불확실성을 숨기지 않는다.

## 적용 범위

이 파일은 저장소 전체에 적용되는 기본 지침입니다.
하위 경로에 별도의 `AGENTS.md`가 존재하면 해당 범위의 세부 지침을 추가로 적용할 수 있습니다.

## 사용자 전용 영역

다음 영역은 기본적으로 사용자 전용으로 간주합니다.

- `human/`
- `notes/`
- `decisions/`
- `roadmap/`
- `backlog/`
- `private/`

사용자가 명시적으로 요청하지 않는 한 위 영역의 문서를 작업 지침으로 해석하지 않습니다.
